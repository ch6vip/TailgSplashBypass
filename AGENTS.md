# Project Instructions

- Never compile, assemble, bundle, or install an APK locally.
- Run Android builds, unit tests, and lint only through GitHub Actions after pushing changes.
- Local source inspection, formatting checks, XML parsing, and other non-build static checks are allowed.
